let fib = \x.
    if x <= 0 then
        0
    else if x = 1 then
        [1, 0]
    else
        let lst = fib (x - 1) in
        let v = lst.(0) + lst.(1) in
        v ++ lst
in

let amount = read in
let res = fib amount in
res
