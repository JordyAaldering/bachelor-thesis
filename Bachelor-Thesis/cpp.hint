// Hint files help the Visual Studio IDE interpret Visual C++ identifiers
// such as names of functions and macros.
// For more information see https://go.microsoft.com/fwlink/?linkid=865984
#define EQUALITY_OP(op) Value Value::operator op(Value other) { for (int i = 0; i < Values.size(); i++) if (!(Values[i] op other.Values[i])) return 0; return 1; }
#define ARITH_OP(op) Value Value::operator op(Value other) { std::vector<double> v; v.reserve(Values.size()); for (int i = 0; i < Values.size(); i++) v.push_back(Values[i] op other.Values[i]); return Value(Dim, Shape, v); }
